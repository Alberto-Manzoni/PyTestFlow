# Step Tests

Covers the library of built-in step decorators. These tests confirm that each
Prefect-task-backed step adheres to the philosophy of using plain functions with
the shared `ptf_context` for data exchange and metadata enrichment.
