# Core Tests

Verifies foundational pieces such as the shared `ptf_context`, the `Step`
wrapper built on Prefect tasks, and the `PyTestflowState` hierarchy. Keeping
these behaviors correct upholds the framework's flow-first execution model.
